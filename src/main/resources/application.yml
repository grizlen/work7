server:
  port: 80
spring:
  datasource:
    url: jdbc:h2:file:./db/server/demodb
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        #        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
